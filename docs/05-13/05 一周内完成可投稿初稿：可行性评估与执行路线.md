# 一周内完成可投稿初稿：可行性评估与执行路线

## 结论

**完全可行。** 一周内完成"可投稿"状态的初稿不仅可行，而且是当前节点的推荐路径。关键前提是：本周的工作范围严格限定为**阶段 0 修正**（无需 GPU、无需新实验），而非等待 A1/H1/G1/S2 全部完成。

---

## 为什么一周足够

当前 PDF 已是 56 页的完整稿件，框架、算法、实验结果、讨论均已就位。所有 V2 实验数据（S1/A2/E1-fix/E2/P1）均已有 CSV 来源。一周内需要完成的全部是**修正现有错误**，而非从头写作。

阶段 0 的 9 项修正任务（问题 A–J）均为纯 LaTeX 编辑或 CSV 读取操作，无需运行任何实验，总工时约 4–6 小时，远低于一周的时间预算。

---

## 一周执行路线（按天分配）

### 第 1 天（约 3–4 小时）：修正数据错误——最高优先级

**任务 A：替换 Table 6 为 A2 staged diagnostic**

这是当前最高优先级。将 `tables/ablation.tex` 完整替换为以下内容（数据来源：`exp_a2_diagnostic_stats.csv`，seeds 41–45，n=5）：

```latex
\begin{table}[t]
\caption{Staged diagnostic ablation (A2) at budget $B = 1.70$
  ($n = 5$ seeds, seeds 41--45). Each row adds one component
  to the previous configuration. Lower FW-MAE is better.}
\label{tab:ablation}
\begin{tabular}{llr}
\toprule
Stage & Configuration & FW-MAE (mean $\pm$ std) \\
\midrule
D1 & MaskedActor + ActionEmbedding          & $0.4224 \pm 0.0326$ \\
D2 & D1 + EventAwareCritic                  & $0.4219 \pm 0.0412$ \\
D3 & D2 + AWBC auxiliary loss               & $0.4080 \pm 0.0284$ \\
D4 & D3 + Oracle-calibrated prior (PD-PPO)  & $0.3942 \pm 0.0384$ \\
\bottomrule
\end{tabular}
\end{table}
```

同步替换 `sections/06_experiments.tex` §6.9 正文，删除所有"remove one component"描述，改为 A2 staged diagnostic 叙述。

**任务 B：从 CSV 修正 Table 2 的 std 值**

读取 `reports/v2_supplement_assets/exp_s1_main_stats.csv`，将 PD-PPO 三行的 std 值更新为 CSV 实际值（σ₂=0.0240 已锁定，σ₁/σ₃ 从 CSV 读取）。同时核查 mean 值是否与锁定值（0.3979/0.3911/0.3907）一致，以 CSV 为准。

**任务 C：修正摘要百分比**

将摘要中"outperforming Age-of-Information scheduling by 8.2%"改为"7.9%"。同时核查"approaching within 0.4% of an oracle-informed static baseline"——根据锁定值 (0.3912−0.3911)/0.3912 ≈ 0.03%，应改为"within 0.03%"（或保留 0.4% 并在正文中说明计算基准）。

---

### 第 2 天（约 2–3 小时）：修正正文叙述

**任务 D：插入统计显著性段落**

在 `sections/06_experiments.tex` §6.3 主结果表之后插入 Wilcoxon + Bonferroni 段落（参见 turn_56 Step 2 的 LaTeX 代码）。核心内容：

```latex
\paragraph{Statistical significance.}
We assess pairwise significance using the Wilcoxon signed-rank test
with Bonferroni correction for six comparisons ($\alpha_{\text{adj}} = 0.05/6 \approx 0.0083$).
PD-PPO significantly outperforms Random at all three budgets ($p < 0.0083$).
At $B = 1.70$ and $B = 1.75$, PD-PPO also significantly outperforms
AoI-based scheduling and Round-robin ($p < 0.0083$).
At $B = 1.65$, the difference between PD-PPO and AoI approaches
but does not reach Bonferroni-corrected significance.
PD-PPO does not significantly outperform Static projection at any budget.
```

**任务 E：修正 §6.4 Budget Sensitivity 数字**

用锁定值重新计算 PD-PPO vs Round-robin 的改进百分比：
- B=1.65: (0.4057−0.3979)/0.4057 ≈ 1.9%
- B=1.70: (0.4041−0.3911)/0.4041 ≈ 3.2%
- B=1.75: (0.4042−0.3907)/0.4042 ≈ 3.3%

替换 §6.4 中的 2.8%/3.1%/2.9%。

**任务 F：从 CSV 修正 Table 3（E1）数字**

读取 `reports/v2_supplement_assets/exp_e1_condition_stats_fixed.csv`，将 Table 3 中所有数字替换为 CSV 实际值（当前 PDF 中 Calm/Mixed/Event 条件下 AoI 等基线数字与锁定值存在偏差）。

---

### 第 3 天（约 2 小时）：修正符号与图形

**任务 G：修正 §3.4 状态定义（e_t → z_t）**

将 `sections/03_problem_formulation.tex` 中 Eq.(7) 的 `e_t` 改为 `z_t`，并在 §5.5 EventAwareCritic 中引入 `z_t` 的定义（`z_t` 为 EventAwareCritic 的输入特征向量，区别于原始事件标志 `e_t`）。

**任务 H：修正 Figure 5 图例**

重新生成 Figure 5（Power-error tradeoff），将图例中的"Custom PPO"改为"PD-PPO"。这是纯绘图操作，约 30 分钟。

---

### 第 4 天（作者操作）：填写占位符

**任务 I：填写传感器功率值**

查阅 OTT Parsivel2 和 Modbus RTU 气象站的设备数据手册，将 `tables/sensor_specs.tex`（Table 1）中的归一化功率占位符替换为真实功率值（瓦特）。同时填写 §4.1 中的 [FILL] 占位符。

**任务 J：填写论文元数据**

将 `paper.tex` 中的 `\author`、`\ead`、`\address`、`\cortext` 占位符替换为真实作者信息。填写 Declaration of Generative AI 中的 [NAME OF TOOL / SERVICE] 和 [REASON]，以及 Acknowledgements 中的资助来源。

---

### 第 5 天（约 2 小时）：全文通读与最终核查

完成上述所有修正后，进行最终通读，重点检查：

1. 所有表格数字与对应 CSV 文件一致（`grep` 验证关键数字）
2. 摘要、正文、表格中的百分比数字三处一致（均为 7.9%）
3. 算法命名全文统一为 PD-PPO（`grep "Custom PPO" sections/*.tex` 返回空）
4. 状态向量符号全文一致（`grep "e_t" sections/03*.tex` 返回空）
5. Table 6 不含"3 seeds"或"remove one component"字样
6. 参考文献 key 变更已同步到正文（`grep "Ying2022\|Chen2026\|Lim2021\|Liu2024" sections/*.tex` 返回正确引用）

---

## 一周后的投稿状态

完成阶段 0 的 9 项修正后，论文达到以下状态：

**数据完整性**：所有表格数字均有 CSV 来源，无无来源数字。

**声称准确性**：所有百分比与锁定值一致，无过强声称（EventAwareCritic 贡献已软化，V2 生成器局限性已说明，统计显著性边界已明确）。

**格式合规性**：elsarticle-harv 格式，符合 Cold Regions Science and Technology 投稿要求。

**消融表**：A2 staged diagnostic（D1–D4，n=5，有 CSV 来源），可支撑"AWBC 贡献最大（−3.3%）"的声称。

这一状态足以通过期刊编辑的初审（desk review），进入同行评审阶段。

---

## 一周内不需要完成的内容

以下内容**不影响投稿**，可在收到审稿意见后作为修改稿补充：

**A1 完整消融（70 runs）**：当前 A2 staged diagnostic 已足够支撑消融分析。A1 可在修改稿阶段提供更强证据，但不是初稿的必要条件。

**H1 超参数扫描（40 runs）**：当前版本已在 §5.7 中说明"sensitivity analysis is future work"，审稿人可能要求补充，但不会因此拒稿。

**G1 + S2（V3 生成器验证与重跑）**：V2 生成器的局限性已在 §4.2 和 §6.5 中明确说明，V3 生成器已在 §7.3 中作为 future work 提及。这是论文的已知局限性，不是致命缺陷。

**T1 跨站点泛化**：已降级为 future work，无需在初稿中完成。

---

## 风险提示

**唯一的实质性风险**是 Table 2 的 mean 值问题（问题 B）：PDF 中 PD-PPO 的 mean 值（0.3930/0.3908/0.3920）与锁定值（0.3979/0.3911/0.3907）存在差异，尤其是 B=1.65 差异较大（0.3930 vs 0.3979）。这需要在第 1 天直接读取 CSV 确认真实值，以 CSV 为准更新表格，不得使用 PDF 中的现有数字。

若 CSV 中的实际值与锁定值也存在差异（例如因不同 seed 集合导致），需在第 1 天确认后统一全文引用的数字来源，确保摘要、正文、表格三处一致。
